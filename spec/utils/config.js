/**
 * Created by pengshuo on 17/6/13.
 */
/*
var NOT_PROD=true;

//todo 不知道怎么设置var NOT_PROD为全局变量。。
 import { config } from "../../src/utils/config";


describe("config", function() {
  //var config = require('../../src/utils/config');

  beforeAll(function(){
    NOT_PROD=true;
  })

  it("to be a object", function() {
    expect(config).toEqual(jasmine.any(Object));
  });

  /!*it("should contain {prefix: 'kdrp'}", function() {
    expect(config).toEqual(jasmine.objectContaining({
      prefix: 'kdrp'
    }));
  });*!/


});
*/
